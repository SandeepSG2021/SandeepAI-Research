# Copy to .env (never commit). PowerShell:
#   Copy-Item .env.example .env
# Or set them as $env:NAME = "..." in the shell.

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=youraddress@gmail.com
SMTP_PASSWORD=your-gmail-app-password
MAIL_FROM=SandeepAI Briefing <youraddress@gmail.com>

# Comma-separated list. Defaults to the two recipients hard-coded in send_email.py.
MAIL_TO=Sandeep.chauhans@gmail.com,Sandeep_singh@manulife.com
