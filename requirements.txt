# Pinned for reproducible, supply-chain-safe builds.
# Bump deliberately; do not use unbounded `>=`.
requests==2.32.5
feedparser==6.0.11
beautifulsoup4==4.13.5
lxml==6.0.2
openai==2.41.1
python-dotenv==1.1.1
